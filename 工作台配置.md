# 工作台配置

> 这份文件是给 AI 看的。以后想改工作台，直接跟 AI 说「给我的工作台加个喝水提醒」，
> 它会读这份配置接着改，不用重新聊一遍。**请不要删掉这个文件。**

生成时间：2026-08-03
最后更新：2026-08-03

---

## 一、用户画像

- **身份 / 主要在忙什么**：冰神。OpenClaw 生态主理人（技术向）；WorldQuant BRAIN Alpha 挖掘者；每日站桩练功；研究八字命理（自建「八字排盘 · 子平盲派双核」网站，落款"冰临城下"）。
- **核心诉求**：站桩/Alpha/八字三块日常记录不中断；语录激励；待办与常用网址随手存取；密码速查。
- **隐私敏感度**：一般偏高——密码本不加锁（信任设备），但备份文件强调勿转发。
- **技术水平**：懂开发，会部署（Netlify / 花生壳静态托管都用过）。

## 二、功能清单

| # | 功能 | 标识 | 类型 | 侧边栏 | 说明 |
|---|---|---|---|---|---|
| 1 | 首页 | `home` | 本地 | ☯️ 方寸（品牌入口，hidden 路由） | 今日干支 + 语录（可追加/换一条）+ 今日一览（站桩连续、今日Alpha、最近待办倒计时） |
| 2 | 站桩 | `zhan` | 本地 | 🧘 站桩 | 每日打卡（时长+感受），连续天数，第几天=基数+记录数（基数在设置里设） |
| 3 | Alpha | `alpha` | 本地 | 📈 Alpha | **双账户**（冰神/涵涵，`owner` 字段区分）；地区 USA/GLB/EUR/ASI/CHN/KOR/HKG/IND/MEA；类型 AOTM/PPA/RA/SA；备注可改；历史可删；初始累计按人分开 |
| 4 | 农历·八字 | `lunar` | 本地 | 📅 农历 | 农历日历（1900-2100 数据表）+ 当日干支 + 八字记录（自动排四柱，立春换年/节气月/23点子初换日/五鼠遁） |
| 5 | 排盘 | `paipan` | 联网·跳转 | 🔮 排盘 | 一键跳掌门自建排盘站（PAIPAN_URL） |
| 6 | 待办 | `todo` | 本地 | ✅ 待办 | 勾选完成；**目标日可选**（自动算剩余天数，就是倒计时） |
| 7 | 密码本 | `pass` | 本地·敏感 | 🔑 密码本 | 账号密码速查、搜索、掩码显示、复制；不加锁 |
| 8 | 江湖 | `links` | 本地 | 🗺️ 江湖 | 常用网址收藏：名号+去处，点卡片跳转，自动补 https:// |
| - | 设置 | `settings` | 本地 | ⚙️ 设置 | 日夜主题（宣纸/玄夜+自动按时间）、初始数据（站桩天数/冰神累计/涵涵累计）、备份恢复、清空 |

**内容型功能的到期日**：无（本工作台无一次性内容型功能）。

**用户明确拒绝/改过的**：
- 倒计时独立页 → 并入待办（目标日）
- 学习资料链接墙 → 删除
- 八字学习 AI 问答 → 改为跳转自建排盘站
- 密码本加锁 → 拒绝（信任设备）
- 天气/新闻等联网 → 未选择
- 生成器署名 → 移除，改为版本号 v1.0.0

## 三、风格参数

选定版本：国风 · 日夜双主题（宣纸 / 玄夜），侧边栏底部 🌙/☀️ 切换 + 按时间自动（19 点入夜、7 点日出，设置可关）

```css
/* 日：宣纸 */
--bg:#f2ece1; --sidebar:#5c4a3d; --sidebar-text:#f2ece1; --sidebar-active:#7a6455;
--card:#fdfaf4; --text:#3b322b; --text-dim:#8c7f73; --accent:#a8443a;
--radius:8px; --shadow:0 2px 10px rgba(90,70,50,.10);
--font:"Songti SC","STSong","Noto Serif SC",serif;
/* 夜：玄夜（body.night） */
--bg:#191512; --sidebar:#221c17; --sidebar-text:#b8a48c; --sidebar-active:#3d332a;
--card:#262019; --text:#ece2d0; --text-dim:#9a8a74; --accent:#c8553d;
--shadow:0 0 0 1px rgba(255,240,220,.07);
```

- 品牌 emoji：☯️
- 工作台名：方寸 ｜ 短名称（桌面图标下）：方寸
- slogan：一切福田，不离方寸（首页标题下 + 无署名）
- 图标做法：深檀底（#5c4a3d）+ 宋体「方」字（Songti.ttc）
- 用户偏好：标题左侧朱砂竖线；深檀版弃用；要"江湖"这类诗意命名

## 四、数据结构

存储命名空间：`bysdash:`（敏感数据 `bysdash$secret:`，不进备份）

| Store key | 结构 | 归属功能 |
|---|---|---|
| `quotes` / `quoteIdx` | `[string]` / `number` | 首页语录 |
| `zhan:YYYY-MM-DD` | `{min, feel, ts}` | 站桩（按天） |
| `zhan_base` | `number` | 站桩第几天基数（设置里设） |
| `alpha:YYYY-MM-DD` | `[{id, owner:'bing'\|'han', region, type, note, ts}]` | Alpha（按天） |
| `alpha_base_bing` / `alpha_base_han` | `number` | Alpha 初始累计（按人） |
| `bazi` | `[{id, name, y, m, d, hb, note, gz:{y,m,d,h}, ts}]` | 八字记录 |
| `todo` | `[{id, text, done, date?, ts}]` | 待办（date=目标日倒计时） |
| `pass` | `[{id, site, account, pwd, note, ts}]` | 密码本 |
| `links` | `[{id, name, url, ts}]` | 江湖收藏 |
| `theme` / `themeAuto` | `'day'\|'night'` / `bool` | 主题 |
| `_lastBackup` / `_lastWrite` / `_lastPage` | 时间戳 / 字符串 | 系统 |

**农历/干支数据表**：`LUNAR_INFO`（1900-2100，经典编码）+ `JQ_STR`（每年 48 字符节气日表）内联在 index.html。**勿手改**，需重新生成时用 lunar_python 提取（注意 `getMonths()` 返回 15 个月含相邻年份，需锚点切分）。

## 五、联网配置

**无 API key**（排盘是纯跳转，apiCard 已删除）。排盘站地址常量 `PAIPAN_URL`。

## 六、部署

- **本地目录**：`/Users/libing/WorkBuddy/2026-08-03-08-55-01/方寸/`
- **源码仓库（GitHub 私有）**：`onefreecomet/fangcun`，SSH 推送（`git@github.com:onefreecomet/fangcun.git`，main 分支）
- **维护流程**：改 index.html → `git commit` → `git push` → 重新拖部署 → 手机刷新
- 层级：L1 已上线
- 平台：花生壳静态托管（drop.ficp.fun）
- 地址：`https://ficp.fun/s/2U2yUe/`
- 部署方式：直接上传文件夹（三文件）
- 已加到桌面：待确认
- 云同步配置：无

## 七、工程状态

- 文件结构：三文件（index.html + manifest.json + icon.png）
- index.html 行数：约 1850（超 1500；刻意不分文件——单文件保证 jsdom 冒烟测试可跑，靠分区标记维护）
- 上次验收脚本：通过（静态检查 + 冒烟 9 项 + 干支对拍 69 项）· 2026-08-03
- 数据/算法已对拍：干支农历与 lunar_python 69 项全过，年天数 1900-2099 全过

## 八、变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-08-03 | 初始生成 v1.0.0，9 功能；Alpha 改 WQ 地区/类型代码、加备注✎、加历史删除、双账户（冰神/涵涵）、初始累计挪设置；站桩加第几天基数+删除；全部删除改自绘确认弹窗；倒计时并入待办；新增江湖（收藏）；首页并入品牌「方寸」；slogan「一切福田，不离方寸」；署名改版本号 |
